import { Action } from '@ngrx/store';
import { MetadataDictionary } from '../../models/dictionary';
export declare enum DictionaryActionTypes {
    InitializeDictionaryMetadata = "[Dictionary] initialize incoming metadata",
    AddDictionaryMetadataList = "[Dictionary] add dictionary metadata list",
    UpdateDictionaryMetadata = "[Dictionary] update dictionary metadata list",
}
export declare class InitializeDictionaryMetadataAction implements Action {
    dictionaryMetadataIdentifiers: Array<string>;
    readonly type: DictionaryActionTypes;
    constructor(dictionaryMetadataIdentifiers: Array<string>);
}
export declare class AddDictionaryMetadataListAction implements Action {
    dictionaryMetadataList: MetadataDictionary[];
    readonly type: DictionaryActionTypes;
    constructor(dictionaryMetadataList: MetadataDictionary[]);
}
export declare class UpdateDictionaryMetadataAction implements Action {
    dictionaryMetadataId: string;
    changes: Partial<MetadataDictionary>;
    readonly type: DictionaryActionTypes;
    constructor(dictionaryMetadataId: string, changes: Partial<MetadataDictionary>);
}
export declare type DictionaryActions = InitializeDictionaryMetadataAction | AddDictionaryMetadataListAction | UpdateDictionaryMetadataAction;
