import { OnInit } from '@angular/core';
import { Store } from '@ngrx/store';
import { Observable } from 'rxjs';
import { MetadataDictionary } from '../../models/dictionary';
import { DictionaryState } from '../../store/reducers/dictionary.reducer';
export declare class DictionaryListComponent implements OnInit {
    private store;
    metadataIdentifiers: Array<string>;
    dictionaryList$: Observable<MetadataDictionary[]>;
    activeItem: number;
    constructor(store: Store<DictionaryState>);
    ngOnInit(): void;
    setActiveItem(index: any, e: any): void;
}
