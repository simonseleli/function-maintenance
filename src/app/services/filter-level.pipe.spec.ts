import { FilterLevelPipe } from './filter-level.pipe';

describe('FilterLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
