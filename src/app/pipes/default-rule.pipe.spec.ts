import { DefaultRulePipe } from './default-rule.pipe';

describe('DefaultRulePipe', () => {
  it('create an instance', () => {
    const pipe = new DefaultRulePipe();
    expect(pipe).toBeTruthy();
  });
});
